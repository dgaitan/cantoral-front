import { test } from "@playwright/test";

test("presentation page loads Reveal.js deck", async () => { test.skip(); });
test("arrow key advances to next slide", async () => { test.skip(); });
test("escape key exits presentation", async () => { test.skip(); });
test("touch swipe navigates on mobile", async () => { test.skip(); });
